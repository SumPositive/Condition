# 症状メモ 設計書

体調メモ（Condition）へ新機能「症状メモ」を追加するための設計。
既存の測定記録（`BodyRecord`）はそのままに、症状を記録・可視化する仕組みを並置する。

最終更新: 2026-09-21

---

## 目次

1. [方針](#1-方針)
2. [用語](#2-用語)
3. [画面構成](#3-画面構成)
4. [データモデル](#4-データモデル)
   - 4.1 [SymptomRecord](#41-symptomrecord)
   - 4.2 [程度（HealthKit 準拠）](#42-程度healthkit-準拠)
   - 4.3 [症状辞書と症状タグリスト](#43-症状辞書と症状タグリスト)
   - 4.4 [症状辞書の内容（46件）](#44-症状辞書の内容46件)
   - 4.5 [薬マスタ](#45-薬マスタ)
5. [記録画面](#5-記録画面)
6. [天候取得（WeatherKit ＋ リワード広告）](#6-天候取得weatherkit--リワード広告)
7. [グラフ](#7-グラフ)
8. [統計](#8-統計)
9. [入出力（JSON / CSV / PDF）](#9-入出力json--csv--pdf)
10. [HealthKit 連携](#10-healthkit-連携)
11. [ローカライズ](#11-ローカライズ)
12. [実装フェーズ](#12-実装フェーズ)
13. [未決事項](#13-未決事項)

---

## 1. 方針

- **1レコード＝1症状＋1程度**。複数症状の同時発生は複数レコードで表す。
  程度・開始終了・薬はすべて症状ごとに異なるため、1レコードに複数症状を詰めると
  「症状別の平均程度」「症状別の平均持続時間」が算出できなくなる。
- **測定記録と症状記録は別モデル**にし、リレーションは張らない。時刻で突き合わせる。
- **記録リストは統合**し、セルの種類で測定／症状を区別する。
  3月11日を開いたときに「血圧 152/94」と「頭痛・強い」が並ぶことが本機能の価値。
- **グラフ・統計はセグメントで切り替える**。連続値と離散イベントで軸が異なるため同居できない。
- 既存コードの命名規約（`n`/`s`/`b` プレフィックス、単位サフィックス、`0` = 未入力）に従う。

---

## 2. 用語

| 用語 | 意味 |
|---|---|
| 症状辞書 | アプリ内蔵の読み取り専用マスタ（46件、分類付き）。ユーザーは編集しない |
| 症状タグリスト | ユーザーが辞書から選んで自分用に並べたリスト。記録画面に出るのはこれ |
| 症状ID | レコードに保存する安定キー。辞書由来は slug、ユーザー追加は `u:<UUID>` |
| エピソード | 1件の症状レコード（開始〜終了） |

---

## 3. 画面構成

タブは4つのまま増やさない（iOS は5つを超えると「その他」に押し込まれる）。

```
記録   ─ 統合リスト（測定セル／症状セル）＋ 絞り込み [すべて | 測定 | 症状]
グラフ ─ セグメント [測定 | 症状]
統計   ─ セグメント [測定 | 症状]
設定   ─ ＋症状タグリスト編集、＋薬マスタ編集、＋天候設定
```

### 3.1 記録タブ（統合リスト）

| | 測定セル | 症状セル |
|---|---|---|
| 左アイコン | 既存の区分アイコン（朝食後など） | 症状アイコン＋程度の色 |
| 主表示 | 数値の並び | 症状名＋程度バッジ |
| 副表示 | メモ・測定場所 | 持続時間／継続中バッジ、薬タグ |
| 地色 | 現状のまま | ごく薄い色でレーン分け |
| スワイプ | 既存 | ＋「終了」（継続中のときのみ） |

実装上の注意:

- `RecordListView` は現在 `@Query` で `BodyRecord` 単体を引いている。
  2モデルを引いてマージした配列（enum でラップした行）に変える。年月セクション分けは流用可。
- 絞り込みセグメントの選択状態は `AppSettings` に持たせ、グラフ・統計タブとは**独立**させる
  （記録は「すべて」で見たいが、グラフは「症状」で見たい、が普通にある）。
- `＋` ボタンは現在2種（通常・平均）。「症状を記録」を足して3種のメニューにする。
  起動時アクション設定にも「症状を記録」を追加。

### 3.2 グラフ・統計タブ

セグメントの選択状態はグラフと統計で**共有**する（`AppSettings.analysisDomain`）。
「いま症状を見ている」という文脈がタブ移動で途切れないようにする。

---

## 4. データモデル

### 4.1 SymptomRecord

```swift
@Model
final class SymptomRecord {
    // MARK: - 日時
    @Attribute(.spotlight) var startAt: Date = Date()
    var endAt: Date? = nil          // nil かつ bOngoing == false なら「点」の記録
    var bOngoing: Bool = false      // 継続中（終了時刻の入力待ち）

    // MARK: - 症状
    var sSymptomID: String = ""     // "headache" / "u:<UUID>"
    var nSeverity: Int = 0          // HKCategoryValueSeverity の rawValue

    // MARK: - 付帯情報
    var sNote: String = ""
    var sMedicineIDs: String = ""   // JSON 配列。例: ["analgesic","u:8F1C-..."]
    var nDataSource: Int = RecordDataSource.appInput.rawValue

    // MARK: - 天候スナップショット（0 / 空 = 未取得）
    var nTemp_10c: Int = 0                 // 気温 x10 ℃
    var nHumidity_p: Int = 0               // 湿度 %
    var nPressure_10hpa: Int = 0           // 気圧 x10 hPa
    var nPressureDelta24h_10hpa: Int = 0   // 24時間前との差 x10 hPa（符号あり）
    var sWeatherSymbol: String = ""        // SF Symbol 名
    var sWeatherPlace: String = ""         // 市区町村レベルの地名
    var nWeatherSource: Int = 0            // 0=なし 1=自動取得 2=手動入力

    init(startAt: Date = Date(), symptomID: String = "") {
        self.startAt = startAt
        self.sSymptomID = symptomID
    }
}
```

`Schema([BodyRecord.self, SymptomRecord.self])` へモデルを1つ足すだけなので、
SwiftData の追加のみのマイグレーションで済む（`ModelContainer+Setup.swift`）。

#### 日時の扱い

- **開始は必須、終了は任意**。症状の持続時間は桁が3つ違う（閃輝暗点 20分／頭痛 半日／花粉症 2か月）
  ため持続時間は分析の主役だが、記録時に終了を必須にすると発症直後に記録できなくなる。
- 開始だけで保存 → 症状セルに「終了」ボタン → 治まったとき1タップで `endAt = now`。
- `bOngoing` が立っている間はセルに継続中バッジを出し、入れ忘れに気づけるようにする。

| bOngoing | endAt | 意味 | 持続時間 |
|---|---|---|---|
| false | nil | 点の記録（終了を記録しなかった） | 統計から除外 |
| true | nil | 継続中 | 「現在まで◯時間」として暫定表示、統計は別枠 |
| false | あり | 完了したエピソード | `endAt - startAt` |

### 4.2 程度（HealthKit 準拠）

`HKCategoryValueSeverity` の rawValue をそのまま保存する。

| rawValue | HealthKit | 表示 | UI |
|---|---|---|---|
| 0 | `.unspecified` | 未指定 | **UI に出さない**（外部データ取り込み専用） |
| 1 | `.notPresent` | なし | 出す |
| 2 | `.mild` | 軽い | 出す |
| 3 | `.moderate` | 中くらい | 出す（新規記録の初期選択） |
| 4 | `.severe` | 強い | 出す |

- 「なし(1)」を残すのは、花粉症のように毎日つける使い方で
  「今日はなかった」と「つけ忘れ」を区別するため。これが無いと頻度統計が壊れる。
- 統計では `0`（未指定）を除外する。
- 変換ロジックは持たない。保存値＝HealthKit 値なので往復で情報が落ちない。

### 4.3 症状辞書と症状タグリスト

二層構造にする。

```
症状辞書（内蔵・読み取り専用・46件・分類付き）
        │ ユーザーが「使う症状」を選択
        ▼
症状タグリスト（ユーザーのマイリスト）
        │ 最終使用日時の降順で並べ替え
        ▼
記録画面のタグ
```

#### 症状辞書

- 46件を分類ごとにグループ化して表示する読み取り専用マスタ。コード内の静的定義。
- 各エントリ: `slug` / 分類 / ローカライズキー / 既定アイコン / 既定色 / HealthKit 識別子（あれば）。

#### 症状タグリスト

- ユーザーが辞書から選んだものだけが入る。辞書に無い症状はユーザー追加で `u:<UUID>` を発行。
- 保持する情報: 症状ID / 表示名の上書き（任意）/ 色・アイコンの上書き（任意）/
  最終使用日時 / 使用回数 / 非表示フラグ。
- **並び順は最終使用日時の降順**。未使用のものは追加順で末尾に並ぶ。
  ユーザーによる手動並べ替えは持たない（使うほど上に来るので不要）。
- 記録画面には上位8件をタグで出し、`＋` で全件（辞書＋ユーザー追加）を検索できるシートを開く。
  シートから選んだ時点でタグリストへ自動追加される。

#### 保存先

`AppSettings` の `DateOptAppearanceStore` と同じく UserDefaults に JSON で持つ。
SwiftData の別テーブルにしない理由はデータ量が小さいことと、既存マスタが全てこの方式であること。

#### レコードには表示名ではなく ID を保存する

日本語の「頭痛」を文字列でレコードに保存すると、英語ユーザーの画面に「頭痛」と出るうえ、
集計キーが言語依存になって統計が言語ごとに割れる。本アプリは ja/en/ko/zh-Hant の4言語対応。
ID 保存ならユーザーが表示名を変えてもレコードの一括置換が不要になる。

### 4.4 症状辞書の内容（46件）

HealthKit 欄は書き出し先の `HKCategoryTypeIdentifier`。空欄はアプリ内のみ。
**識別子の正確な綴りと利用可否は実装時に SDK で要確認**（近似マッピングを含む）。

| 分類 | slug | 日本語 | HealthKit |
|---|---|---|---|
| 頭・神経 | `headache` | 頭痛 | `.headache` |
| 頭・神経 | `dizziness` | めまい | `.dizziness` |
| 頭・神経 | `lightheadedness` | ふらつき・立ちくらみ | — |
| 頭・神経 | `numbness` | しびれ | — |
| 頭・神経 | `scintillatingScotoma` | 閃輝暗点 | — |
| 頭・神経 | `insomnia` | 不眠 | `.sleepChanges` |
| 頭・神経 | `drowsiness` | 眠気 | `.sleepChanges` |
| 全身 | `fever` | 発熱 | `.fever` |
| 全身 | `fatigue` | 倦怠感・だるさ | `.fatigue` |
| 全身 | `chills` | 悪寒 | `.chills` |
| 全身 | `nightSweats` | 寝汗 | `.nightSweats` |
| 全身 | `edema` | むくみ | — |
| 全身 | `coldSensitivity` | 冷え | — |
| 消化器 | `abdominalPain` | 腹痛 | `.abdominalCramps`（近似） |
| 消化器 | `nausea` | 吐き気 | `.nausea` |
| 消化器 | `diarrhea` | 下痢 | `.diarrhea` |
| 消化器 | `constipation` | 便秘 | `.constipation` |
| 消化器 | `heartburn` | 胸やけ | `.heartburn` |
| 消化器 | `bloating` | 腹部膨満 | `.bloating` |
| 消化器 | `appetiteLoss` | 食欲不振 | `.appetiteChanges`（値型が異なる・要検討） |
| 呼吸器・耳鼻 | `cough` | 咳 | `.coughing` |
| 呼吸器・耳鼻 | `phlegm` | 痰 | — |
| 呼吸器・耳鼻 | `runnyNose` | 鼻水・鼻づまり | `.runnyNose` |
| 呼吸器・耳鼻 | `soreThroat` | のどの痛み | `.soreThroat` |
| 呼吸器・耳鼻 | `shortnessOfBreath` | 息切れ | `.shortnessOfBreath` |
| 呼吸器・耳鼻 | `hayFever` | 花粉症 | — |
| 呼吸器・耳鼻 | `tinnitus` | 耳鳴り | — |
| 呼吸器・耳鼻 | `earFullness` | 耳の閉塞感 | — |
| 筋・骨格 | `backPain` | 腰痛 | `.lowerBackPain` |
| 筋・骨格 | `stiffShoulder` | 肩こり | — |
| 筋・骨格 | `neckPain` | 首の痛み | — |
| 筋・骨格 | `jointPain` | 関節痛 | — |
| 筋・骨格 | `musclePain` | 筋肉痛 | `.generalizedBodyAche`（近似） |
| 循環器 | `palpitations` | 動悸 | `.rapidPoundingOrFlutteringHeartbeat` |
| 循環器 | `chestPain` | 胸の痛み | `.chestTightnessOrPain` |
| 皮膚 | `itching` | かゆみ | — |
| 皮膚 | `rash` | 発疹・湿疹 | — |
| 眼 | `eyeStrain` | 目の疲れ | — |
| 眼 | `blurredVision` | かすみ目 | — |
| 眼 | `floaters` | 飛蚊症 | — |
| 口腔 | `toothache` | 歯痛 | — |
| 口腔 | `dryMouth` | 口の渇き | — |
| 精神 | `lowMood` | 気分の落ち込み | `.moodChanges` |
| 精神 | `anxiety` | 不安 | — |
| 精神 | `irritability` | イライラ | — |
| 女性 | `menstrualPain` | 生理痛 | `.pelvicPain`（近似） |

### 4.5 薬マスタ

症状タグリストと同じ仕組みを使い回す（辞書＋マイリスト＋MRU 並べ替え）。

**プリセットは薬効分類で持ち、商品名はユーザー追加**とする。
商品名は国ごとに違う（ロキソニンは日本、Advil は米国）ため、4言語アプリのプリセットには入れられない。

プリセット（17件）:
解熱鎮痛薬 / 胃腸薬 / 整腸薬 / 下痢止め / 便秘薬 / 抗アレルギー薬 / 点鼻薬 / 目薬 /
鎮咳去痰薬 / 総合感冒薬 / 湿布・外用鎮痛薬 / 睡眠改善薬 / 酔い止め / 漢方 /
ビタミン・サプリ / 降圧薬 / その他の処方薬

レコードには `sMedicineIDs`（JSON 配列）で複数保存する。症状と違い、薬は同時に複数飲むのが普通。

---

## 5. 記録画面

```
┌──────────────────────────────┐
│ キャンセル          症状      保存 │
├──────────────────────────────┤
│ 開始  2026/9/21(月) 14:30      │
│ 終了  ─ （継続中トグル）        │
├──────────────────────────────┤
│ 症状                          │
│ [頭痛][肩こり][めまい][花粉症]  │  ← MRU 上位8件
│ [腰痛][耳鳴り][不眠][吐き気][＋]│
├──────────────────────────────┤
│ 程度  なし │ 軽い │ 中 │ 強い   │  ← 初期選択は「中」
├──────────────────────────────┤
│ 薬                            │
│ [解熱鎮痛薬][抗アレルギー薬][＋]│
├──────────────────────────────┤
│ メモ                          │
│ [                          ]  │  ← AZMemoEditor を流用
├──────────────────────────────┤
│ 天候                          │
│ 20.1℃ 62% 1008.3hPa ☁ 前日-6.2 │
│ [自動取得（広告）] [手動入力]    │
└──────────────────────────────┘
```

- メモ欄は既存の `AZMemoEditor` をそのまま使う。
- 保存後に「続けて記録」ボタンを出し、日時・天候・薬を引き継いで2件目を作れるようにする
  （複数症状の同時発生を2タップで記録するための導線）。

---

## 6. 天候取得（WeatherKit ＋ リワード広告）

### 6.1 WeatherKit

- 取得できるもの: 気温・湿度・気圧・天気シンボル。**過去日も取得可能**
  （`weather(for:including:.hourly(startDate:endDate:))`）。
- 必要な準備:
  - App ID で WeatherKit を有効化、Xcode で Capability 追加
  - `NSLocationWhenInUseUsageDescription` を `Info.plist` に追加
  - `PrivacyInfo.xcprivacy` の更新
- 費用: 月50万コールまで無料。記録ごとに1回なら到達しない。
- **Apple Weather の帰属表示（ロゴ＋法的リンク）が義務**。天候値を表示する画面すべてに必要。
  審査で見られる。

### 6.2 設計上の要点

1. **取得値はレコードにスナップショット保存し、後から再取得しない。**
   過去の API には遡れる期間の限りがあり、通信できない場所もある。
2. **気圧は絶対値より変化量。** 頭痛との相関が言われているのは「気圧が下がる局面」なので、
   hourly を取って `nPressureDelta24h_10hpa`（24時間前との差）も同時に保存する。
   後から計算しようとすると過去の気圧を引き直す羽目になる。
3. **位置は市区町村レベルに丸めて保存。** プライバシー・審査説明・統計用途のいずれでも十分。
4. **手動入力の経路は常に残す**（位置情報を拒否、機内モード、過去分の後入力）。
   `nWeatherSource` で自動／手動を区別する。
5. 過去日の記録に付ける場合は「現在地の、その日時の天候」になる。
   当時どこにいたかは分からないため、その旨を画面に明示する。

### 6.3 リワード広告によるゲート

**クレジットは持たない。広告を開始し、視聴が完了した時点で取得を実行する。**
残数管理も繰り越しもないので、ユーザーから見て「広告を見た ＝ いま天候が入る」が1対1で対応する。

```
[自動取得（広告）] タップ
   │
   ├─ 事前条件チェック（広告より前に必ず行う）
   │    ├ 位置情報の許可     → 未許可なら許可を求める。拒否されたら手動入力へ誘導して終了
   │    └ ネットワーク到達性 → 不通なら「通信できません」で終了
   │
   ├─ リワード広告をロード
   │    ├ 成功 → 表示
   │    │         ├ 視聴完了 → 天候を取得して画面へ反映
   │    │         └ 中断     → 何も起きない（再タップで最初から）
   │    └ 失敗 → 広告なしで取得を実行する（後述）
   │
   └─ 取得が失敗した場合 → シート内に「再試行」を出す。この再試行は広告なし
```

#### 要点

1. **事前条件は広告より前に確かめる。**
   位置情報の許可ダイアログや圏外判定を広告のあとに回すと、
   「広告を見たのに天候が入らない」が構造的に発生する。順序を逆にしない。
2. **視聴完了後の取得失敗は、広告なしの再試行を許す。**
   クレジットではなく、その記録シートを閉じるまで有効な一時的な状態として持つ（回数制限なし）。
   通信が一瞬切れただけでもう一度広告、という体験を避けるため。
3. **広告のロードに失敗したときは取得を実行する。**
   配信在庫が無い状況はユーザーの責任ではなく、ここを塞ぐと機能が理由なく死ぬ。
   なお「通信を切って広告を回避する」ことはできない（広告が出ない状況では天候取得も失敗する）ため、
   寛容にしても抜け道にならない。
4. **手動入力は常に無料・無制限。** 天候なしでも症状メモは完全に使える状態を保つ。
   天候は付加価値であって前提ではない。
5. 広告は AdMob の `RewardedAd`。Google Mobile Ads SDK はバナーで導入済みなので追加は容易
   （`AdMobViews.swift` にはバナーのみ実装されている）。同意管理は既存の `AdConsentManager` を流用。

#### 取得の単位

1回の視聴で取得できるのは**その記録1件分の天候**。
「続けて記録」で2件目を作るときは、1件目の天候スナップショットをそのまま引き継ぐ
（同一時刻・同一地点なので再取得の必要がない。広告も出さない）。

## 7. グラフ（優先度順）

| # | 内容 | 狙い |
|---|---|---|
| 1 | **症状カレンダー（ヒートマップ）** | 月表示、日ごとの色の濃さ＝その日の最大程度、複数症状は色分けドット。「月に何回」が一目で分かる本機能の顔 |
| 2 | **症状別 発生回数の推移** | 週次／月次の積み上げ棒。分類別時系列。花粉症の季節性が出る |
| 3 | **エピソード帯グラフ** | 各発生を横帯で描く。閃輝暗点の点と花粉症の2か月が並ぶと持続時間の差が直感的に分かる |
| 4 | **程度の時系列（折れ線）** | 症状を選んで表示。既存の `GraphPeriod` をそのまま流用 |
| 5 | **時刻分布** | 既存 `Bp24HChartView` の流儀を踏襲。「頭痛は朝に多い」 |
| 6 | **曜日別・月別の発生数** | 生活リズムと季節性 |
| 7 | **測定グラフへの症状オーバーレイ** | 血圧の折れ線に症状発生日のマーカー。統合リストの思想をグラフにも通す |

---

## 8. 統計（優先度順）

| # | 内容 | 狙い |
|---|---|---|
| 1 | **頻度サマリー** | 症状別に「期間内◯回 / 月あたり◯回 / 平均持続◯時間 / 平均程度 / 最終発生日 / 前期間比」。要件の「月数回・年数回」の答え |
| 2 | **発生間隔** | 平均間隔・最長無症状期間。頭痛持ちが最も見たい数字は「何日空いたか」 |
| 3 | **気圧との相関** | 発生日と非発生日で「24時間気圧変化」の分布を比較。散布図より2群比較（平均差＋箱ひげ）が読みやすい |
| 4 | **気温・湿度との相関** | 同じ枠組み |
| 5 | **測定値との相関** | 症状あり日 vs なし日で血圧・体温・体重の平均を比較。散布図に症状あり点を色分けでも可 |
| 6 | **時間帯・曜日・月の偏り** | 棒グラフ＋「20時台が最多（全体の31%）」の一文 |
| 7 | **薬との関係** | 薬あり／なしで平均持続時間と平均程度を比較 |
| 8 | **併発しやすい症状ペア** | 同日に一緒に出る組み合わせ（頭痛×めまい など） |

### 設計ルール

- **各群の件数が閾値（5件）を下回るときは数値を出さず「データが不足しています」に倒す。**
  n=2 で「気圧と強い相関」と出すのは誤読を生む。
- 文言は「傾向」「◯件中◯件」に留め、**原因・診断を示唆する表現は使わない**。
  医療アプリとしての安全性と App Store 審査の双方で効く。

---

## 9. 入出力（JSON / CSV / PDF）

### JSON（`RecordsJSONIO`）

- `currentSchemaVersion` を上げ、封筒に `symptoms` 配列と `symptomTags`（タグリストのマスタ）を追加。
- **フェーズ1の時点で出力に対応させること。** 先にデータが溜まってから対応すると、
  その間にバックアップを取ったユーザーの症状データが欠ける。
- 既存の `"condition"` / `"conditionRaw"` と同じ流儀で、症状は ID と表示名を両方出す。

```json
{
  "schemaVersion": 3,
  "records": [ ... ],
  "symptoms": [
    {
      "startAt": "2026-09-21T14:30:00+09:00",
      "endAt": "2026-09-21T18:00:00+09:00",
      "ongoing": false,
      "symptom": "頭痛",
      "symptomId": "headache",
      "severity": 3,
      "note": "",
      "medicines": ["解熱鎮痛薬"],
      "medicineIds": ["analgesic"],
      "weather": {
        "source": "auto",
        "temp": 20.1, "humidity": 62,
        "pressure": 1008.3, "pressureDelta24h": -6.2,
        "symbol": "cloud.fill", "place": "横浜市"
      }
    }
  ],
  "symptomTags": [ ... ]
}
```

### CSV / PDF

列構成が違うため測定と症状を**混ぜない**。1ファイル内で
「測定セクション／症状セクション」に分けるか、ファイルを分ける。

---

## 10. HealthKit 連携

- 既存 `HealthKitService` は `HKQuantityType` のみ扱っている。症状は `HKCategoryType` なので別実装。
- 程度は `HKCategoryValueSeverity` をそのまま保存しているため変換不要。
- 辞書の HealthKit 欄が空の症状（閃輝暗点・飛蚊症・花粉症・耳鳴り・歯痛・肩こり 等）は
  書き出し対象外。アプリ内のみで扱う。
- 実装フェーズは最後。まずアプリ内で完結させる。

---

## 11. ローカライズ

対応言語: ja / en / ko / zh-Hant（`Localizable.xcstrings`）。

新規に必要な訳:

| 対象 | 件数 | 備考 |
|---|---|---|
| 症状辞書 | 46 × 4言語 | 分類名11個も別途 |
| 薬プリセット | 17 × 4言語 | |
| 程度ラベル | 4 × 4言語 | |
| 画面文言 | 未定 | |

症状名は医学用語と日常語の中間を狙う。「鼻漏」ではなく「鼻水・鼻づまり」。

---

## 12. 実装フェーズ

| フェーズ | 内容 |
|---|---|
| 1 | `SymptomRecord` / 症状辞書 / 症状タグリスト / 薬マスタ / 記録シート / 統合リストの症状セル / JSON 入出力（天候は手動入力のみ） |
| 2 | グラフ: 症状カレンダー＋発生回数推移 |
| 3 | 統計: 頻度サマリー＋発生間隔 |
| 4 | WeatherKit 自動取得（Capability・位置許可・帰属表示・リワード広告ゲート） |
| 5 | 相関統計、CSV/PDF 出力、測定グラフへの症状オーバーレイ |
| 6 | HealthKit 症状カテゴリ連携 |

---

## 13. 未決事項

- 症状セルのアイコン。46件すべてに固有の SF Symbol を割り当てるか、分類ごとの共通アイコンにするか
- 継続中レコードの通知（「頭痛は終わりましたか？」）を出すか。出すなら通知許可が増える
- 広告のロード失敗時に取得を通す方針（6.3-3）で問題ないか
- 症状タグリストの上限件数を設けるか
- `appetiteLoss` の HealthKit マッピング（`.appetiteChanges` は値型が `HKCategoryValueAppetiteChanges` で
  severity ではないため、書き出し対象外にするかどうか）
