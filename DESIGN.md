# 症状メモ 設計書

体調メモ（Condition）へ新機能「症状メモ」を追加するための設計。
既存の測定記録（`BodyRecord`）を生かし、症状を記録・可視化する仕組みを並置する。

最終更新: 2026-09-22

---

## 目次

1. [方針](#1-方針)
2. [用語](#2-用語)
3. [画面構成](#3-画面構成)
4. [データモデル](#4-データモデル)
   - 4.1 [SymptomRecord](#41-symptomrecord)
   - 4.1.1 [BodyRecord に追加する項目](#411-bodyrecord-に追加する項目)
   - 4.2 [程度（HealthKit 準拠）](#42-程度healthkit-準拠)
   - 4.3 [症状マスタと症状タグリスト](#43-症状マスタと症状タグリスト)
   - 4.4 [プリセット症状の内容（10件）](#44-プリセット症状の内容10件)
   - 4.4.1 [入力の上限](#441-入力の上限)
   - 4.5 [対処マスタ（薬・薬以外）](#45-対処マスタ薬薬以外)
5. [記録画面](#5-記録画面)
   - 5.1 [測定・症状で共用する環境シート](#51-測定症状で共用する環境シート)
6. [環境データの取得](#6-環境データの取得)
7. [グラフ](#7-グラフ)
8. [統計](#8-統計)
9. [入出力（JSON / CSV / PDF）](#9-入出力json--csv--pdf)
10. [HealthKit 連携](#10-healthkit-連携)
11. [ローカライズ](#11-ローカライズ)
12. [実装フェーズ](#12-実装フェーズ)
13. [類似アプリから参考にする点](#13-類似アプリから参考にする点)
14. [未決事項](#14-未決事項)

---

## 1. 方針

- **1レコード＝1症状＋1程度**。複数症状の同時発生は複数レコードで表す。
  程度・開始終了・薬はすべて症状ごとに異なるため、1レコードに複数症状を詰めると
  「症状別の平均程度」「症状別の平均持続時間」が算出できなくなる。
- **測定記録と症状記録は別モデル**にし、リレーションは張らない。時刻で突き合わせる。
- **記録リストは統合**し、セルの種類で測定／症状を区別する。
  3月11日を開いたときに「血圧 152/94」と「頭痛・強い」が並ぶことが本機能の価値。
- **グラフ・統計はセグメントで切り替える**。連続値と離散イベントで軸が異なるため同居できない。
- 既存コードの命名規約（`n`/`s`/`b` プレフィックス、単位サフィックス、`0` = 未入力）に従う。

---

## 2. 用語

| 用語 | 意味 |
|---|---|
| プリセット | アプリ内蔵の初期値（症状10件・対処10件）。ユーザーは名前を変更でき、消しても構わない |
| 症状タグリスト | プリセットとユーザー追加を区別せず持つ単一マスタ。画面に出るのはこれ |
| 症状ID | レコードに保存する安定キー。辞書由来は slug、ユーザー追加は `u:<UUID>` |
| エピソード | 1件の症状レコード（開始〜終了） |

---

## 3. 画面構成

タブは4つのまま増やさない（iOS は5つを超えると「その他」に押し込まれる）。

```
記録   ─ 統合リスト（測定セル／症状セル）＋ 絞り込み [すべて | 測定 | 症状]
グラフ ─ セグメント [測定 | 症状]
統計   ─ セグメント [測定 | 症状]
設定   ─ ＋症状タグリスト編集、＋薬マスタ編集、＋環境設定
```

### 3.1 記録タブ（統合リスト）

| | 測定セル | 症状セル |
|---|---|---|
| 左アイコン | 既存の区分アイコン（朝食後など） | 継続＝砂時計／終息＝チェック |
| 主表示 | 従来の身体測定値の並び。環境値は入れない | 症状名＋程度バッジ（どちらも文字） |
| 副表示 | メモ・測定場所 | 持続時間、対処、メモ |
| 地色 | 現状のまま | ごく薄い色でレーン分け |

#### 継続／終息の切り替え

日付列は測定セルと同じく「日付」と「マーク列（`catW` = 24pt）」に分ける。
症状セルはそのマーク列に継続状態のアイコンを置く。

- 24pt に「継続」の文字は入らないので**アイコンで表す**。
  継続＝砂時計（アクセント色）、終息＝チェック（グレー）。読み上げは従来どおり文字で伝える。
- タップすると確認ダイアログが出る。**タイトルが現在の状態、選択肢は切り替えの1つだけ。**
  - 継続中: 「現在も継続中です」／[終息しました]
  - 終息済: 「終息しました」／[継続中に戻す]
  - 現状のままにしたいときはキャンセルで閉じる。
- **終息済みにもマークを出す。** 間違えて閉じたものを継続へ戻す導線が他にないため。
- 継続へ戻すときは `endAt` を `nil` にする。残すと `needsEnding` が false のままで
  見た目が終息のままになり、持続時間も止まった値で出てしまう。
- 行内に「終息」ボタンは置かない。症状名と幅を取り合い、狭い端末や大きい文字で
  バッジの中の語が折り返してしまっていた。

実装上の注意:

- `RecordListView` は現在 `@Query` で `BodyRecord` 単体を引いている。
  2モデルを引いてマージした配列（enum でラップした行）に変える。年月セクション分けは流用可。
- 絞り込みセグメントの選択状態は `AppSettings` に持たせ、グラフ・統計タブとは**独立**させる
  （記録は「すべて」で見たいが、グラフは「症状」で見たい、が普通にある）。
- `＋` ボタンは現在2種（通常・平均）。「症状を記録」を足して3種のメニューにする。
  起動時アクション設定にも「症状を記録」を追加。

### 3.2 グラフ・統計タブ

セグメントの選択状態はグラフと統計で**共有**する（`AppSettings.analysisDomain`）。
「いま症状を見ている」という文脈がタブ移動で途切れないようにする。

---

## 4. データモデル

### 4.1 SymptomRecord

```swift
@Model
final class SymptomRecord {
    // MARK: - 日時
    @Attribute(.spotlight) var startAt: Date = Date()
    var endAt: Date? = nil          // nil かつ bOngoing == false なら「点」の記録
    var bOngoing: Bool = false      // 継続中（終了時刻の入力待ち）

    // MARK: - 症状
    var sSymptomID: String = ""     // "headache" / "u:<UUID>"
    var nSeverity: Int = 0          // HKCategoryValueSeverity の rawValue

    // MARK: - 付帯情報
    var sNote: String = ""
    var sMedicineIDs: String = ""   // JSON 配列。例: ["analgesic","u:8F1C-..."]
    var nDataSource: Int = RecordDataSource.appInput.rawValue

    // MARK: - 環境スナップショット
    var nTemp_10c: Int = 0                 // 気温 x10 ℃
    var bWeatherTempPresent: Bool = false  // 0℃と未取得を区別
    var nHumidity_p: Int = 0               // 湿度 %
    var bWeatherHumidityPresent: Bool = false // 0%と未取得を区別
    var nPressure_10hpa: Int = 0           // 気圧 x10 hPa、0=未取得
    var nPressureDelta24h_10hpa: Int = 0   // 24時間前との差 x10 hPa（符号あり）
    var bPressureDelta24hPresent: Bool = false // 差が0と計算不可を区別
    var sWeatherSymbol: String = ""        // SF Symbol 名
    var sWeatherPlace: String = ""         // 市区町村レベルの地名
    var sWeatherStationID: String = ""     // 気象庁の観測所番号、他の取得元では空
    var sWeatherSourceURL: String = ""     // 取得した気象庁データの出典URL
    var nWeatherSource: Int = 0            // 0=なし 1=WeatherKit 2=手動入力 3=気象庁
    var nIndoorTemp_10c: Int = 0           // 室内の気温 x10 ℃
    var nIndoorHumidity_p: Int = 0         // 室内の湿度 %
    var bIndoorTempSet: Bool = false       // 室内の気温が入力済み
    var bIndoorHumiditySet: Bool = false   // 室内の湿度が入力済み
    var bWeatherTempManual: Bool = false   // 気温のみ手動変更
    var bWeatherHumidityManual: Bool = false // 湿度のみ手動変更
    var bWeatherPressureManual: Bool = false // 観測所の気圧を手動変更

    // MARK: - 端末の気圧（環境スナップショットとは別に保存）
    var nDevicePressure_10hpa: Int = 0     // 測定場所の気圧 x10 hPa、0=未取得

    init(startAt: Date = Date(), symptomID: String = "") {
        self.startAt = startAt
        self.sSymptomID = symptomID
    }
}
```

`Schema([BodyRecord.self, SymptomRecord.self])` へ症状モデルを追加する（`ModelContainer+Setup.swift`）。
環境と端末気圧の導入時には既存の `BodyRecord` にも既定値付き項目を追加するため、
両モデルの保存済みデータを維持できるかマイグレーション時に確認する。

#### 4.1.1 BodyRecord に追加する項目

```swift
// 身体測定値と分けて、測定記録の付帯情報として保存する
var nWeatherTemp_10c: Int = 0
var bWeatherTempPresent: Bool = false
var nWeatherHumidity_p: Int = 0
var bWeatherHumidityPresent: Bool = false
var nWeatherPressure_10hpa: Int = 0
var nWeatherPressureDelta24h_10hpa: Int = 0
var bPressureDelta24hPresent: Bool = false
var sWeatherSymbol: String = ""
var sWeatherPlace: String = ""
var sWeatherStationID: String = ""
var sWeatherSourceURL: String = ""
var nWeatherSource: Int = 0
var nIndoorTemp_10c: Int = 0
var nIndoorHumidity_p: Int = 0
var bIndoorTempSet: Bool = false
var bIndoorHumiditySet: Bool = false
var bWeatherTempManual: Bool = false
var bWeatherHumidityManual: Bool = false
var bWeatherPressureManual: Bool = false

// 端末の現地気圧は観測所の気圧とは別に保存する
var nDevicePressure_10hpa: Int = 0
```

環境と端末気圧は体温の `nTemp_10c` や血圧とは別の付帯情報で、平均測定の各試行値には含めない。
平均測定レコードにも各1件のスナップショットを付けられる。
既存の症状データでは気温・湿度の入力有無フラグがないため、移行時は既存の入力値から設定し、
値が0の旧レコードは未取得として扱う。

#### 日時の扱い

- **開始は必須、終了は任意**。症状の持続時間は桁が3つ違う（閃輝暗点 20分／頭痛 半日／花粉症 2か月）
  ため持続時間は分析の主役だが、記録時に終了を必須にすると発症直後に記録できなくなる。
- 開始だけで保存 → 症状セルに「終了」ボタン → 治まったとき1タップで `endAt = now`。
- `bOngoing` が立っている間はセルに継続中バッジを出し、入れ忘れに気づけるようにする。

| bOngoing | endAt | 意味 | 持続時間 |
|---|---|---|---|
| false | nil | 点の記録（終了を記録しなかった） | 統計から除外 |
| true | nil | 継続中 | 「現在まで◯時間」として暫定表示、統計は別枠 |
| false | あり | 完了したエピソード | `endAt - startAt` |

### 4.2 程度（HealthKit 準拠）

`HKCategoryValueSeverity` の rawValue をそのまま保存する。

| rawValue | HealthKit | 表示 | UI |
|---|---|---|---|
| 0 | `.unspecified` | 未指定 | **UI に出さない**（外部データ取り込み専用） |
| 1 | `.notPresent` | なし | 出す |
| 2 | `.mild` | 軽い | 出す |
| 3 | `.moderate` | 中くらい | 出す（新規記録の初期選択） |
| 4 | `.severe` | 強い | 出す |

- 「なし(1)」を残すのは、花粉症のように毎日つける使い方で
  「今日はなかった」と「つけ忘れ」を区別するため。これが無いと頻度統計が壊れる。
- 統計では `0`（未指定）を除外する。
- 変換ロジックは持たない。保存値＝HealthKit 値なので往復で情報が落ちない。

### 4.3 症状マスタと症状タグリスト

```
プリセット（内蔵・10件）
        │ 初回起動時にタグリストへ入れる
        ▼
症状タグリスト（プリセットとユーザー追加を区別しない単一マスタ）
        │ 最終使用日時の降順で並べ替え
        ▼
症状シートのタグ
```

#### プリセットとユーザー追加を区別しない

**画面上も内部の扱いも、両者に差を設けない。** 並び順・色・編集のすべてで同じ規則に従う。

- プリセットは10件のみ。個人が実際に記録する症状は限られており、
  多すぎると目的の症状を探すほうが手間になる。足りないものはユーザーが足す前提。
- 並びは日本の有訴者率（肩こり・腰痛が男女とも上位）と、
  体調記録アプリで扱う中心（頭痛・倦怠感・めまい）を踏まえた順。
- **プリセットも名前を変更できる。** 変更するとその1言語ぶんだけ保存され、
  端末の言語を変えても変更後の名前のままになる。
  これは「区別をなくす」ことを優先した結果として受け入れる。
- **分類（頭・神経／全身／消化器 …）は持たない。** 実際に使う症状が数個しかない
  個人利用では、分類見出しばかりが並んで探しにくかった。対処と同じく1つの列に並べる。

#### 症状タグリスト

- プリセットもユーザー追加も同じリストに入る。辞書に無い症状は `u:<UUID>` を発行。
- 保持する情報: 症状ID / 表示名の上書き（任意）/ 色の上書き（任意）/
  最終使用日時 / 使用回数 / 非表示フラグ。
- **色は ID から機械的に決める**（`SymptomPalette`）。分類ごとの色分けをやめたため。
  `hashValue` は起動ごとに変わるので使えず、バイト列の総和を使う。
  同じ ID なら常に同じ色になり、ユーザー追加分にも自動で色が付く。
- **タグは削除せず非表示にする。** ユーザー追加タグを実体ごと消すと、そのタグを使った
  過去の記録は `u:<UUID>` を持ったまま名前を引く先を失い、一覧に UUID が出てしまう。
  どの記録からも参照されていないものだけは `purgeUnusedUserDefined(usedIDs:)` で実体を消せる。
- **並び順は最終使用日時の降順**。未使用のものはプリセットの定義順、
  ユーザー追加はその後に並ぶ。手動並べ替えは持たない（使うほど上に来るので不要）。
- 名前の引き先が無い ID は一覧に出さず、表示が必要な場面では「（不明な項目）」を出す。
  プリセットを入れ替えたときに、生の ID が画面に出るのを防ぐ。

#### タグ数の上限

`SymptomLimits.maxTagsPerList = 100`（症状・対処それぞれ）。

- UserDefaults に JSON で丸ごと持ち起動のたびに読むため、青天井にはしない。
- **手入力の追加は上限で止め、理由を伝える。** 黙って追加されないのが最も困る。
- **インポートは上限を超えても受け入れ、`trimToLimit()` で収める。**
  機械的に先頭から切ると使用中のタグが落ちて過去の記録が名前を引けなくなるので、
  最終使用日時の新しい順に残し、落ちるのを未使用のタグへ寄せる。
- `markUsed` はリストに無い ID を自動追加するので、ここでも上限を尊重する。
  「いま使ったもの」は必ず残したいため、追加してから `trimToLimit()` を呼ぶ。

#### 保存先

`AppSettings` の `DateOptAppearanceStore` と同じく UserDefaults に JSON で持つ。
SwiftData の別テーブルにしない理由はデータ量が小さいことと、既存マスタが全てこの方式であること。

#### レコードには表示名ではなく ID を保存する

日本語の「頭痛」を文字列でレコードに保存すると、英語ユーザーの画面に「頭痛」と出るうえ、
集計キーが言語依存になって統計が言語ごとに割れる。本アプリは ja/en/ko/zh-Hant の4言語対応。
ID 保存ならユーザーが表示名を変えてもレコードの一括置換が不要になる。

### 4.4 プリセット症状の内容（10件）

HealthKit 欄は書き出し先の `HKCategoryTypeIdentifier`。空欄はアプリ内のみ。
**識別子の正確な綴りと利用可否は実装時に SDK で要確認**（近似マッピングを含む）。

並びがそのまま画面の初期表示順になる（未使用のうちは定義順に並ぶため）。

| # | slug | 日本語 | HealthKit |
|---|---|---|---|
| 1 | `stiffShoulder` | 肩こり | — |
| 2 | `backPain` | 腰痛 | — |
| 3 | `headache` | 頭痛 | `.headache` |
| 4 | `fatigue` | 倦怠感・だるさ | `.fatigue` |
| 5 | `dizziness` | めまい | `.dizziness` |
| 6 | `abdominalPain` | 腹痛 | `.abdominalCramps` |
| 7 | `runnyNose` | 鼻水・鼻づまり | `.runnyNose` |
| 8 | `cough` | 咳 | `.coughing` |
| 9 | `fever` | 発熱 | `.fever` |
| 10 | `insomnia` | 不眠 | `.sleepChanges` |

**この10件は「最初に並べるもの」であって、扱える症状の全部ではない。**
ここに無い症状はユーザーが名前を打って追加し、プリセットと同じマスタに入る。

### 4.4.1 入力の上限

| 対象 | 上限 | 適用 |
|---|---|---|
| メモ | 200文字 | 入力時に止める |
| メモ（バックアップ取り込み） | 2000文字 | 壊れたデータを弾くためだけの値 |
| タグ名（追加・リネーム） | 20文字 | 入力時に止める |
| 1記録あたりの対処 | 10件 | 選択時に止める |
| タグリストの件数 | 100件 | 手入力は止めて知らせる／取り込みは使用順で切る |

**取り込みの上限を入力の上限と同じにしない。** 過去に長いメモを保存していた利用者の
バックアップを復元したときに本文が切り捨てられてしまうため、取り込みは緩くする。

保存時に黙って切ると打った文と違うものが残るので、**上限は入力の時点で止める**。
メモは残り30文字を切ったら残数を表示する。

### 4.5 対処マスタ（薬・薬以外）

症状タグリストと**同じ仕組み・同じ画面**を使う（`SymptomPickerSheet` を共用）。
プリセットとユーザー追加を区別しない点も、利用順で並べる点も症状と同じ。

記録画面では**「対処」1行**にまとめ、薬と薬以外（寝た・安静・通院など）を
同じタグリストから複数選べるようにする。

**プリセットは薬効分類で持ち、商品名はユーザー追加**とする。
商品名は国ごとに違う（ロキソニンは日本、Advil は米国）ため、4言語アプリのプリセットには入れられない。

プリセット（10件）— 症状プリセット10件に合わせ、実際に取る手をこれだけでカバーする:

| # | slug | 日本語 |
|---|---|---|
| 1 | `analgesic` | 解熱鎮痛薬 |
| 2 | `sleep` | 寝た |
| 3 | `rest` | 安静にした |
| 4 | `warming` | 温めた |
| 5 | `cooling` | 冷やした |
| 6 | `gastric` | 胃腸薬 |
| 7 | `antiallergy` | 抗アレルギー薬 |
| 8 | `bath` | 入浴した |
| 9 | `stretch` | ストレッチ・体操 |
| 10 | `clinicVisit` | 受診した |

市販薬は主要3系統（解熱鎮痛薬・胃腸薬・抗アレルギー薬）に絞る。
総合感冒薬・鎮咳去痰薬・点鼻薬・目薬などは、必要な人が名前を足す。

**「何もしなかった」は持たない。** 未選択と意味が重なるため。

レコードには `sMedicineIDs`（JSON 配列）で複数保存する。症状と違い、対処は同時に複数あるのが普通。

**薬と薬以外は区別しない**（2026-09-22 判断）。
当初は統計7「薬あり／なしで持続時間を比較」のために種別を持たせたが、
薬は17分類あり、解熱鎮痛薬と目薬をまとめて「薬」として効果を比べても意味がない。
実際に知りたいのは「この対処で楽になったか」で、それはタグ単位の比較で足りる。
種別を持つと、辞書外のユーザー追加タグにも種別が要り、追加時に選ばせる手間と
「体操した」が薬に分類される事故を生んだため撤去した。

---

## 5. 記録画面

```
┌──────────────────────────────┐
│ キャンセル          症状      保存 │
├──────────────────────────────┤
│ 発症  2026/9/21(月) 14:30      │
│ 終息  ─○  (?)                 │
│ 環境  (?)            未設定 ＞ │
├──────────────────────────────┤
│ 症状              [頭痛]    ＞ │  ← 選択済みだけ／タップでシート
│ 程度  なし │ 軽い │ 中 │ 強い   │  ← 初期選択は「中」
│ 対処  [解熱鎮痛薬][入浴した] ＞ │  ← 選択済みだけ／タップでシート
├──────────────────────────────┤
│ メモ                          │
│ [気づいたことなど 最大200字]   │  ← AZMemoEditor を流用
└──────────────────────────────┘
```

- **症状・程度・対処は1つのセクションにまとめる。** 一続きの入力であり、
  見出しを置くと行のラベルと同じ語が重複するため、セクション見出しは持たない。
- **記録画面には候補を並べず、選択済みだけを出す。** 以前は画面にも MRU 上位8件を
  並べていたが、シートと二段構えになり「どちらで選ぶのか」が分かりにくかった。
  選択・解除・追加・編集はすべてシート側に集約する。
- **環境・症状・対処はセル全体をタップ領域にする。** 環境行だけは、ヘルプの (?) を
  押せるようにラベルを Button の外へ出す必要があるため、行全体に `onTapGesture` を敷く
  （(?) は Button なので親のタップジェスチャより先に受け取る）。
- 未選択のときは症状・対処とも「未選択」と出す。
- メモ欄は既存の `AZMemoEditor` をそのまま使う。上限文字数はプレースホルダーに埋め込み、
  定数（`noteMaxLength`）から組み立てて文言と実装がずれないようにする。
- 保存後に「続けて記録」ボタンを出し、日時・環境・薬を引き継いで2件目を作れるようにする
  （複数症状の同時発生を2タップで記録するための導線）。
- 症状と通常・平均の測定画面には同じ「環境」ボタンを置き、押した時だけ共通のシートを開く。詳しくは5.1に定める。
- 室内の気温・湿度は屋外の観測値と別の欄に手入力し、屋外値を上書きしない。
  屋外値には「観測所の屋外値」、室内値には「室内・手入力」と示す。
- 気象庁の観測所に気圧がない場合、観測所の気圧と24時間変化量は「欠測」と表示する。
  端末の気圧は独立して取得・表示し、観測所の気圧欄へ自動転記しない。
- 「続けて記録」で複製する場合は同時発生の症状に限って端末気圧を引き継ぎ、再取得もできるようにする。

### 5.0 症状・対処を選ぶシート

症状と対処で同じ `SymptomPickerSheet` を使う。どちらも `.large` で開く。

```
┌──────────────────────────────┐
│ ∨          症状を選ぶ          │  ← 対処は右上に「完了」
├──────────────────────────────┤
│ (?) タグをタップで選択、長押しで編集 │
│ 探して無ければ追加  ← 編集中は「タグ編集」│
│ [名前            ]      追加   │  ← 編集中は「変更」
│  編集をやめる     ← 編集中のみ  │
├──────────────────────────────┤
│ [肩こり][腰痛][頭痛][倦怠感]…  │  ← 利用順の1列
└──────────────────────────────┘
```

- **症状は選んだら閉じる**（1記録1症状）。**対処は閉じず、もう一度押すと解除**できる。
  対処は複数選べるので、続けて選べるほうが速い。
- **「完了」は閉じるだけで、選択はタップした時点で反映済み。**
  下スワイプで閉じても結果は同じ（「完了を押さないと取り消される」作りにはしない）。
- **入力欄は追加と編集の二役。** タグを長押しするとその名前が欄に載り、
  「追加」が「変更」に変わる。編集専用のシートは持たない
  （シートの上にシートを重ねると、どれを操作しているのか分からなくなるため）。
- **List の行に置く Button は `.buttonStyle(.borderless)` を明示する。**
  既定のままだと行全体のタップとして扱われ、同じ行に複数あるとどれも反応しない。
- キーボードは、スクロールで下げるほかキーボード上のアイコンからも閉じられる。
  **入力欄の外をタップして閉じる方式は採らない**。List に重ねたタップジェスチャが
  行内の Button とタップを奪い合い、「変更」が効かなくなるため。

### 5.1 測定・症状で共用する環境シート

- 症状、通常・平均の測定入力・編集画面には「環境」ボタンだけを置き、環境の値や入力欄をフォームへ常時並べない。
  いずれも同じ「環境」シートを開き、「屋外の気象」「室内の気温・湿度」「端末の気圧」に分けて表示する。
  屋外の気象には気温・湿度・観測所の気圧・取得元を含める（天気は記録しない。6.2 参照）。
- 国内の新規記録で環境値が未設定のシートを初めて開いた時は、現在地から選んだ観測所のアメダスJSONを自動取得し、
  得られた気温・湿度・気圧を編集可能な初期値として表示する。端末の気圧も同じシート内に独立した欄と取得ボタンを置く。
  前回の取得成功から1時間以内に取り直す場合は広告を挟む（6.7）。
- 気象庁のデータを表示する時は、シート右下の固定フッターに「出典：気象庁ホームページ」を常時読める大きさで表示し、
  取得した観測データのURLへリンクする。値を編集した場合は「一部手入力」、24時間の気圧差を計算した場合は
  「気象庁データをもとに体調メモで算出」を併記し、変更後や計算後の値を気象庁の観測値として見せない。
  気象庁のデータを一切使わない手入力や端末気圧だけの時は、気象庁の出典を表示しない。
- 自動取得の対象時刻は測定日時または症状の開始日時とする。過去日時の記録で該当時刻のJSONを取得できない場合は欠測とし、
  現在の観測値で埋めない。所在地は現在地から選ぶため、過去に別の場所にいた場合は観測所を確認できる表示にする。
- 同じ新規記録でシートを開き直す時や保存済み記録を編集する時は、編集中・保存済みの値を表示し、
  自動取得で入力済みの値を上書きしない。取得失敗時は欠測を表示し、シート内で再試行または手入力できる。
- シートは編集中のレコードの環境値を読み込む。シートの「完了」で入力内容を元の画面へ戻し、
  元の画面の保存時にレコードへ反映する。シートの「キャンセル」では変更を破棄する。
- シート内の入力状態と取得処理は共通化し、読み書き先だけを `BodyRecord` と `SymptomRecord` で切り替える。
  測定では気温を `nWeatherTemp_10c`、症状では `nTemp_10c` に保存する。
- 環境値は測定・症状のどちらにも付けられる任意の付帯情報とする。
  `BodyRecord` では血圧・脈拍・体温などの身体測定項目に加えず、「気温」と「体温」もラベルで区別する。
- 環境項目は測定項目の表示設定、「測定を追加」の試行列、平均計算、HealthKitへの測定値書き出しに含めない。
  環境値がすべて未取得でも、従来どおり身体測定値だけで記録できる。
- 記録一覧の測定セルは従来の身体測定値を主表示とし、環境値は測定記録を編集する時に「環境」シートで確認する。
  測定値のグラフ・統計にも環境値を測定項目として混ぜず、環境との関係は別の分析として扱う。

---

## 6. 環境データの取得

### 6.1 端末の気圧

- 気圧計を利用できる端末では `CMAltimeter.isRelativeAltitudeAvailable()` を確認し、
  `CMAltitudeData.pressure` を取得する。単位 kPa を hPa に換算して `x10 hPa` で保存する。
- 測定記録・症状記録のどちらも任意の操作でその場の値を取得する。通信・位置情報・WeatherKit・広告は不要。
  非対応端末や取得失敗時は未取得のままにし、理由を表示する。
- 取得時刻の専用項目は設けず、測定は `BodyRecord.dateTime`、症状は `SymptomRecord.startAt` を端末気圧の時刻として扱う。
  取得操作は現在日時の記録でのみ有効にする。過去日時への後入力や過去の記録の編集では取得できず、
  取得後に記録日時を変更した場合は端末気圧を消去する。
- 端末気圧は端末がある高さの現地気圧であり、気象庁の観測所気圧や WeatherKit の気圧とは別に表示・保存する。
  階や標高が変わると値も変わるため、両者を同じ時系列や相関分析に混ぜない。
- 端末気圧だけから24時間変化量を補完しない。比較する両時点の実測値と測定場所・高さの整合を確認できる設計になるまで、
  観測所の `nPressureDelta24h_10hpa` とは混ぜない。
- 実装時は `NSMotionUsageDescription` を設定し、取得処理は必要な値を得たら停止する。

### 6.2 国内: 気象庁の観測データ

- 日本国内の自動取得は[気象庁のアメダスJSON](https://www.jma.go.jp/bosai/amedas/const/amedastable.json)を使う。
  観測所一覧と地点別観測値を組み合わせる。地点別観測値のURL形式は
  `www.jma.go.jp/bosai/amedas/data/point/{観測所番号}/{yyyyMMdd}_{HH}.json`。
  **`{HH}` は 00/03/06/09/12/15/18/21 の3時間区切りのみ**（2026-09-22 実測）。
  例: 11時40分の値は `20260922_09.json` に入っている。1ファイルに3時間ぶんが10分刻みで入る。
  最新観測時刻は `www.jma.go.jp/bosai/amedas/data/latest_time.txt` で取れる。
  シートを開いた時に位置情報の利用許可を確認し、緯度・経度から気温と湿度を観測する近隣の観測所を選ぶ。
  観測所番号と取得したデータのURLをレコードに保存する。位置情報を利用できない時は自動取得せず手動入力を案内する。
  観測所の値であり、記録場所そのものの実測値ではないことを画面に示す。
- **気圧を観測している地点は全1286か所のうち154か所しかない**（2026-09-22 実測）。
  気温914か所・湿度840か所と比べて極端に少なく、ほぼ気象台のある都市部に限られる。
  気温が取れる地点から最寄りの気圧観測所までは中央値23.5km、3割が30km超、最大94km。
  同一観測所に限ると地方在住者は気圧が常に欠測になり、気圧相関の統計が永久に空になる。
- そこで**気温・湿度と気圧で観測所を分けて選ぶ**。気温・湿度は最寄り、気圧は「気圧を観測している最寄り」から取る。
  気圧側の観測所名と距離を画面に必ず表示し、別地点の値であることを隠さない。
  気圧変化は広域で同期するため、数十km離れていても変化傾向の分析には使える（絶対値の比較はしない）。
  気圧観測所が遠すぎる場合（目安100km超）は欠測にする。
- 気圧を取得できた場合だけ、**同じ気圧観測所**の24時間前の値から差を計算する（実測で取得可能を確認済み）。
  気象庁の気圧は通常は海面気圧だが一部の観測所は現地気圧なので、絶対値の地点間比較はしない。
- **天気（晴れ・曇り等）は記録しない**（2026-09-22 判断）。理由は3つ。
  1. 雨が体調に効く主因は気圧低下と湿度上昇で、どちらも既に数値で持っている。
     `気圧1003hPa・湿度85%` のほうが `雨` より情報量が多く、統計にそのまま使える。
  2. 天気は9分類のカテゴリ変数で、各群5件の下限（8章の設計ルール）を満たすのに記録数が要る。
     気圧は連続値なので少数の記録でも傾向が見える。
  3. 自動取得できない。地点別JSONの `weather` キーは全1286地点中151地点にしかなく、
     さらに**そのコードの意味を特定できなかった**。気象庁の天気記号表（項番1〜24）とは値が合わず、
     0起点・1起点のどちらで解釈しても実観測値と矛盾する
     （値0が140地点あるが表では「快晴」は自動観測対象外。値7の網走は視程20km・湿度83%で
     「霧」にも「高い地ふぶき」にも当たらない）。
     推測で対応表を作れば誤った天気が観測値として保存され、後から気づいても直せない。
  相関統計（フェーズ5）を作ったうえで気圧・湿度だけでは物足りなければ、その時点で再検討する。
- 気象庁のサイト用JSONと、公式に案内される[過去データCSV](https://www.data.jma.go.jp/developer/)は提供方法が異なる。
  **サイト用JSONの保持期間は約9日**（2026-09-22 時点で 09-13 は取得可・09-12 は404）。
  保証された仕様ではないので、**取得できなければ欠測にし、失敗を異常として扱わない**。
  10日以上前の日時を記録する場合は自動取得の対象外とし、手動入力へ案内する。
  過去分を画面用JSONへ大量に遡及リクエストしない。
- 公開データにAPI従量料金はない。取得頻度を抑え、同じ観測所・時間帯の応答はキャッシュする。
  [気象庁の利用条件](https://www.jma.go.jp/jma/kishou/info/coment.html)に従い、出典と加工の有無を環境シートに表示し、
  [過去データ画面の過度な自動アクセスの注意](https://www.data.jma.go.jp/risk/obsdl/)も守る。

### 6.3 国外: WeatherKit

- 取得できるもの: 気温・湿度・気圧・天気シンボル。**過去日も取得可能**
  （`weather(for:including:.hourly(startDate:endDate:))`）。
- 必要な準備:
  - App ID で WeatherKit を有効化、Xcode で Capability 追加
  - `NSLocationWhenInUseUsageDescription` を `Info.plist` に追加
  - `PrivacyInfo.xcprivacy` の更新
- 日本国外の記録で使用し、国内の気象庁取得失敗時には自動で切り替えない。
- 費用と無料枠は実装時に最新の契約条件を確認する。
- **Apple Weather の帰属表示（ロゴ＋法的リンク）が義務**。環境値を表示する画面すべてに必要。
  審査で見られる。

### 6.4 環境シート（測定・症状で共用）

天候・室内・端末気圧は**1つの「環境」シート**にまとめ、測定記録と症状記録の両方から開く。
記録画面には要約と「環境」ボタンだけを置き、入力の中身はシート側が持つ。

- 値の受け渡しは `EnvironmentSnapshot`（SwiftData に依存しない素の構造体）で行う。
  記録の型に依存しないので、`BodyRecord` 側を足すときも同じシートをそのまま使える。
- シートは「キャンセル／完了」で閉じ、完了時だけ呼び出し元へスナップショットを返す。
- セクションは 屋外（天候）／室内／端末の気圧計 の3つ。

### 6.5 欠測と有効値の区別

0℃・0%・気圧変化量0はいずれも実際に起こりうる有効値なので、値では欠測と区別できない。
屋外・室内とも**入力有無フラグ**を別に持つ。

| 値 | フラグ |
|---|---|
| `nTemp_10c` | `bTempSet` |
| `nHumidity_p` | `bHumiditySet` |
| `nPressureDelta24h_10hpa` | `bPressureDelta24hSet` |
| `nIndoorTemp_10c` | `bIndoorTempSet` |
| `nIndoorHumidity_p` | `bIndoorHumiditySet` |

気圧の絶対値（`nPressure_10hpa`）だけは物理的に0にならないので値で判定してよい。

気象庁から取得した値は、根拠をたどれるよう出典URL（`sWeatherSourceURL`）も残す。
気圧を手で直した場合は観測値ではなくなるため、24時間変化量・気圧観測所ID・出典URLをまとめて捨てる。

JSON バックアップにもフラグを出力する。旧バックアップにはフラグが無いので、
読み込み時は「値があれば入力済み」とみなして復元する。

### 6.6 気象データの取得・室内値の扱い

1. 測定・症状のどちらも、環境シートに初期表示した取得値とユーザーの変更をレコードにスナップショット保存する。
   取得元は `nWeatherSource`（0=なし、1=WeatherKit、2=全項目手動、3=気象庁）で区別する。
   気象庁の場合は観測所番号を保存する。気温・湿度と気圧で観測所が異なりうるので**両方の番号を持つ**。
2. 取得した屋外の気温・湿度・観測所の気圧はシート内で個別に編集できる。
   屋外値の項目別の手動変更フラグを保存し、変更していない項目の気象庁由来は維持する。
   室内の気温・湿度は別の項目として手入力し、屋外値を上書きしない。
   室内値も項目別の入力有無フラグとともに保存する。
   気圧を手動変更した場合は24時間変化量を消去し、気象庁の気圧分析から除外する。
   室内値と屋外値を同じ系列として相関分析しない。
3. 気温0℃・湿度0%・気圧差0hPaは有効値なので、入力有無フラグで欠測と区別する。
   気象庁の品質情報で欠測・利用不可と示された値も保存しない。
4. 24時間気圧変化量は、同じ取得元・同じ観測所で両時刻の気圧がそろう場合だけ保存する。
   気象庁の観測所に気圧がなければ差も欠測とする。
5. 保存する位置は市区町村レベルに丸める。手動入力は位置情報拒否・圏外・後入力でも使えるよう残す。
   気象庁の気圧が欠測した場合も、手動入力した値は「手入力」として明示し、観測値として扱わない。
   過去日の自動取得は「現在地付近の観測所における、その日時の気象データ」になるため、当時の所在地とは限らないことを示す。
6. 分析時に後からまとめて取得する場合も、取得元の公開範囲で得られる値だけを使い、欠測を推測値で埋めない。

### 6.7 気象データ取得のリワード広告ゲート

**前回の取得成功から1時間**は、続けて取得するのに動画広告の視聴を求める。
取得先が気象庁でも WeatherKit でも同じ扱いにする。

目的は収益（バナー以外の収入源）で、結果として短時間の連打も抑える。
2026-09-23 の方針変更前は「国内の気象庁データは無料なので広告を付けない」としていたが、
取得先によって条件が変わるのは利用者から見て分かりにくく、
無料で何度でも叩ける状態は気象庁への負荷の面でも望ましくないため揃えた。

**クレジットは持たない。**
残数管理も繰り越しもないので、ユーザーから見て「広告を見た ＝ いま屋外の気象データが入る」が1対1で対応する。

```
[今ここ付近の気象データを取得する] タップ
   │
   ├─ 前回の取得成功から1時間以内か？
   │    ├ いいえ（初回を含む）→ そのまま取得へ
   │    └ はい → 広告ゲートへ
   │
   ├─ 事前条件チェック（広告より前に必ず行う）
   │    ├ 位置情報の許可     → 未許可なら許可を求める。拒否されたら手動入力へ誘導して終了
   │    └ ネットワーク到達性 → 不通なら「通信できません」で終了
   │
   ├─ リワード広告をロード
   │    ├ 成功 → 表示
   │    │         ├ 視聴完了 → 気象データを取得して画面へ反映
   │    │         └ 中断     → 何も起きない（再タップで最初から）
   │    └ 失敗 → 広告なしで取得を実行する（後述）
   │
   └─ 取得が失敗した場合 → シート内に「再試行」を出す。この再試行は広告なし
```

#### 要点

1. **1時間の起点は「取得に成功した時刻」。**
   間隔をアメダスの更新（10分）より長く取るのは、同じ値を取り直させないため。
   失敗も数えると、通信が悪かっただけで次の取得に広告が要る状態になってしまう。
   保存先は端末ローカル（UserDefaults）。記録ごとではなくアプリ全体で1つ持つ。
2. **事前条件は広告より前に確かめる。**
   位置情報の許可ダイアログや圏外判定を広告のあとに回すと、
   「広告を見たのに気象データが入らない」が構造的に発生する。順序を逆にしない。
3. **視聴完了後の取得失敗は、広告なしの再試行を許す。**
   クレジットではなく、その記録シートを閉じるまで有効な一時的な状態として持つ（回数制限なし）。
   通信が一瞬切れただけでもう一度広告、という体験を避けるため。
4. **広告のロードに失敗したときは取得を実行する。**
   配信在庫が無い状況はユーザーの責任ではなく、ここを塞ぐと機能が理由なく死ぬ。
   なお「通信を切って広告を回避する」ことはできない（広告が出ない状況では気象データの取得も失敗する）ため、
   寛容にしても抜け道にならない。
5. **端末気圧には広告を付けない。** 通信を伴わず、外部サービスへの負荷も無い。
   取得ボタンは気象データと共通で、端末気圧は「今」の記録でだけ続けて測る
   （気圧計は過去に遡れないため、過去日時の記録では黙って飛ばす）。
6. **手動入力は常に無料・無制限。** 環境なしでも症状メモは完全に使える状態を保つ。
   環境は付加価値であって前提ではない。
7. **広告を否定的に見せる表現を使わない。** AdMob のポリシーに抵触しうるため、
   警告記号（⚠️）や「わずらわしい」「回避する」といった書き方は避け、
   音声についても「音が出る場合があります」と事実だけを書く（断定しない）。
   ボタンの文言も「動画」ではなく「動画広告」とし、広告であることを隠さない。
8. 広告は AdMob の `RewardedAd`。Google Mobile Ads SDK はバナーで導入済みなので追加は容易
   （`AdMobViews.swift` にはバナーのみ実装されている）。同意管理は既存の `AdConsentManager` を流用。

#### 実装状況（2026-09-23）

1時間の判定と分岐までは実装済み。広告そのものは未実装で、
`EnvironmentEditView` の確認ダイアログが仮に置いてある
（「動画を見て取得する」を押すと `grantAdReward()` を呼んでそのまま取得する）。
`RewardedAd` を入れる際は、視聴完了のコールバックで `grantAdReward()` を呼ぶ形に差し替える。

#### 取得の単位

1回の視聴で取得できるのは**その記録1件分の屋外の気象データ**。
「続けて記録」で2件目を作るときは、1件目の環境スナップショットをそのまま引き継ぐ
（同一時刻・同一地点なので再取得の必要がない。広告も出さない）。

## 7. グラフ（優先度順）

| # | 内容 | 狙い |
|---|---|---|
| 1 | **症状カレンダー（ヒートマップ）** | 月表示、日ごとの色の濃さ＝その日の最大程度、複数症状は色分けドット。「月に何回」が一目で分かる本機能の顔 |
| 2 | **症状別 発生回数の推移** | 週次／月次の積み上げ棒。分類別時系列。花粉症の季節性が出る |
| 3 | **エピソード帯グラフ** | 各発生を横帯で描く。閃輝暗点の点と花粉症の2か月が並ぶと持続時間の差が直感的に分かる |
| 4 | **程度の時系列（折れ線）** | 症状を選んで表示。既存の `GraphPeriod` をそのまま流用 |
| 5 | **時刻分布** | 既存 `Bp24HChartView` の流儀を踏襲。「頭痛は朝に多い」 |
| 6 | **曜日別・月別の発生数** | 生活リズムと季節性 |
| 7 | **測定グラフへの症状オーバーレイ** | 血圧の折れ線に症状発生日のマーカー。統合リストの思想をグラフにも通す |

---

## 8. 統計（優先度順）

| # | 内容 | 狙い |
|---|---|---|
| 1 | **頻度サマリー** | 症状別に「期間内◯回 / 月あたり◯回 / 平均持続◯時間 / 平均程度 / 最終発生日 / 前期間比」。要件の「月数回・年数回」の答え |
| 2 | **発生間隔** | 平均間隔・最長無症状期間。頭痛持ちが最も見たい数字は「何日空いたか」 |
| 3 | **気圧との相関** | 発生日と非発生日で、取得元・観測所がそろい24時間変化を計算できる記録だけを比較。散布図より2群比較（平均差＋箱ひげ）が読みやすい |
| 4 | **気温・湿度との相関** | 屋外の観測値と室内の手入力値を分けて比較 |
| 5 | **測定値との相関** | 症状あり日 vs なし日で血圧・体温・体重の平均を比較。散布図に症状あり点を色分けでも可 |
| 6 | **時間帯・曜日・月の偏り** | 棒グラフ＋「20時台が最多（全体の31%）」の一文 |
| 7 | **対処との関係** | 対処タグごとに、その対処をした記録としなかった記録で平均持続時間と平均程度を比較 |
| 8 | **併発しやすい症状ペア** | 同日に一緒に出る組み合わせ（頭痛×めまい など） |

### 設計ルール

- **各群の件数が閾値（5件）を下回るときは数値を出さず「データが不足しています」に倒す。**
  n=2 で「気圧と強い相関」と出すのは誤読を生む。
- 文言は「傾向」「◯件中◯件」に留め、**原因・診断を示唆する表現は使わない**。
  医療アプリとしての安全性と App Store 審査の双方で効く。
- 気象庁の気圧が欠測した記録は気圧相関から除外し、端末の現地気圧では補完しない。
  気温・湿度も室内の変更値と観測所の屋外値を混ぜず、集計対象件数を表示する。

---

## 9. 入出力（JSON / CSV / PDF）

### JSON（`RecordsJSONIO`）

- `currentSchemaVersion` を上げ、封筒に `symptoms` 配列と `symptomTags`（タグリストのマスタ）を追加。
- **フェーズ1の時点で出力に対応させること。** 先にデータが溜まってから対応すると、
  その間にバックアップを取ったユーザーの症状データが欠ける。
- 既存の `"condition"` / `"conditionRaw"` と同じ流儀で、症状は ID と表示名を両方出す。

```json
{
  "schemaVersion": 3,
  "records": [
    {
      "dateTime": "2026-09-21T14:30:00+09:00",
      "devicePressure": 1002.4,
      "weather": {
        "source": "jma", "stationId": "観測所番号",
        "conditionId": "clear", "conditionManual": true,
        "temp": 24.0, "tempManual": false,
        "humidity": 45, "humidityManual": false,
        "indoorTemp": 22.0, "indoorHumidity": 50
      }
    }
  ],
  "symptoms": [
    {
      "startAt": "2026-09-21T14:30:00+09:00",
      "endAt": "2026-09-21T18:00:00+09:00",
      "ongoing": false,
      "symptom": "頭痛",
      "symptomId": "headache",
      "severity": 3,
      "note": "",
      "medicines": ["解熱鎮痛薬"],
      "medicineIds": ["analgesic"],
      "devicePressure": 1002.4,
      "weather": {
        "source": "jma", "stationId": "観測所番号",
        "conditionId": "clear", "conditionManual": true,
        "temp": 24.0, "tempManual": false,
        "humidity": 45, "humidityManual": false,
        "indoorTemp": 22.0, "indoorHumidity": 50,
        "place": "横浜市"
      }
    }
  ],
  "symptomTags": [ ... ]
}
```

### CSV / PDF

列構成が違うため測定と症状を**混ぜない**。1ファイル内で
「測定セクション／症状セクション」に分けるか、ファイルを分ける。
JSON では測定・症状の両レコードに環境と端末気圧を任意項目として入れ、旧データに無い場合は未取得とする。
気象庁のデータを使った場合は観測所番号と出典URLも出力し、復元後も出典リンクを表示できるようにする。
気圧が欠測した場合は `pressure` と `pressureDelta24h` を出力しない。気圧を手動変更した場合は `pressureManual` を出力する。
旧JSONの `source: "auto"` は WeatherKit として読み込む。
CSV / PDF にも環境の取得元・観測所番号・屋外値の手動変更フラグ・室内の気温と湿度・「端末の気圧」を独立した項目として出す。
気象庁データを表示するPDFには出典URLと加工の有無を記載し、CSVには出典URLと手動変更フラグを含める。
端末気圧の時刻はそれぞれの記録日時を使う。

---

## 10. HealthKit 連携

- 既存 `HealthKitService` は `HKQuantityType` のみ扱っている。症状は `HKCategoryType` なので別実装。
- 程度は `HKCategoryValueSeverity` をそのまま保存しているため変換不要。
- HealthKit 欄が空の症状（肩こり・腰痛、およびユーザー追加のすべて）は書き出し対象外。
  アプリ内のみで扱う。
- 実装フェーズは最後。まずアプリ内で完結させる。

---

## 11. ローカライズ

対応言語: ja / en / ko / zh-Hant（`Localizable.xcstrings`）。

新規に必要な訳:

| 対象 | 件数 | 備考 |
|---|---|---|
| プリセット | (症状10 + 対処10) × 4言語 | 分類は持たない |
| 薬プリセット | 17 × 4言語 | |
| 程度ラベル | 4 × 4言語 | |
| 画面文言 | 未定 | |

症状名は医学用語と日常語の中間を狙う。「鼻漏」ではなく「鼻水・鼻づまり」。

---

## 12. 実装フェーズ

| フェーズ | 内容 |
|---|---|
| 1 | `SymptomRecord` / 症状辞書 / 症状タグリスト / 薬マスタ / 記録シート / 統合リストの症状セル / JSON 入出力 / 測定・症状共通の環境シート（環境値は手動入力のみ）/ 端末気圧 |
| 2 | グラフ: 症状カレンダー＋発生回数推移 |
| 3 | 統計: 頻度サマリー＋発生間隔 |
| 4 | **症状シートに気象庁取得・端末気圧・室内の気温と湿度を実装済み**（位置許可・出典表示・気圧観測所の距離表示・保持期間外の案内）。1時間ゲートの判定と分岐も実装済み。残り: 測定記録側の環境シート、国外用 WeatherKit、リワード広告の本実装 |
| 5 | 相関統計、CSV/PDF 出力、測定グラフへの症状オーバーレイ |
| 6 | HealthKit 症状カテゴリ連携 |

---

## 13. 類似アプリから参考にする点

2026年9月に公開情報を確認。以下は体調メモへ取り込む設計上の要点であり、各アプリの機能をそのまま再現する方針ではない。

| アプリ | 参考にする点 | 体調メモでの扱い |
|---|---|---|
| [Guava](https://guavahealth.com/health-insights) | 症状・測定値・薬・環境を横断して見られること、関連と因果を区別する説明 | 統合リストと症状マーカーを優先し、相関統計には件数と「傾向」の表現を付ける |
| [CareClinic](https://careclinic.io/features/) | 症状・測定・服薬を時系列と受診用レポートにまとめること | PDFに期間内の症状頻度・程度・持続時間と同期間の測定値を簡潔に並べる |
| [Bearable](https://bearable.app/support-archive/) | 「症状なし」と未記録を区別し、日々の入力を簡単にすること | 程度「なし」を維持し、よく使う症状の素早い入力と確認済み日を検討する |
| [MyTherapy](https://www.mytherapyapp.com/how-apps-help-managing-hypertension-medication-blood-pressure-monitoring) | 服薬・症状・血圧などを同時に記録して振り返ること | 薬タグは記録の文脈として扱う。薬の影響を分析する前に服用時刻の記録を検討する |
| [Migraine Buddy](https://migrainebuddy.com/weather-migraine-webinar-updates-migraine-buddy/) | 頭痛の経過と気圧変化を見やすくすること | 気圧の絶対値に加えて変化量を保存し、症状の開始・終了と並べて表示する |
| [My Health Diary](https://apps.apple.com/jp/app/my-health-diary/id6760221863) | 測定・症状・服薬を同じアプリ内で扱い、月単位でも振り返ること | 既存の測定機能を生かし、症状カレンダーと統合リストを入口にする |

### 分析に必要な記録

- 症状レコードにだけ環境を保存しても、非発生日の環境は得られない。気圧・気温・湿度との比較を実装する前に、非発生日の環境を取得・保存する方法を決める
- 測定レコードの環境を非発生日側の比較に利用する場合も、「測定した日」と「無症状を確認した日」は同義としない
- 端末気圧は任意の記録時点でしか得られないため、未記録日や高さの異なる記録を含めた単純な日別比較には使わない
- 記録がない日を無症状日とみなさない。程度「なし」と未記録を区別し、比較に使える確認済み日の定義を決める
- 薬タグだけでは服用時刻・量・服用前後の程度が分からない。薬あり／なしの集計から効果を示唆しない
- 測定値との比較では、同日というだけでなく症状の開始時刻に対する測定時刻の扱いを明示する

## 14. 未決事項

- 継続中レコードの通知（「頭痛は終わりましたか？」）を出すか。出すなら通知許可が増える
- 広告のロード失敗時に取得を通す方針（6.7-4）で問題ないか
- プリセットの名前を変更すると、その項目だけ端末の言語に追従しなくなる点を
  ユーザーに伝えるか（現在は伝えていない。区別をなくす方針を優先した）

解決済み:

- ~~症状タグリストの上限件数を設けるか~~ → 100件（4.4.1）
- ~~`appetiteLoss` の HealthKit マッピング~~ → プリセットから外したため対象外
